{
  "name": "backend",
  "version": "1.0.0",
  "description": "fastAPI + SQLAlchemy backend for wurwolves",
  "main": "index.js",
  "license": "None",
  "scripts": {
    "start": "run-script-os",
    "start:default": "./venv/Scripts/uvicorn app.main:app --reload",
    "start:win32": ".\\venv\\Scripts\\uvicorn app.main:app --reload",
    "test": "./venv/Scripts/pytest",
    "install": "run-script-os",
    "install:default": "python -m venv venv && venv/bin/pip install -r requirements.txt",
    "install:win32": "python -m venv venv && venv\\Scripts\\pip install -r requirements.txt"
  },
  "dependencies": {
    "run-script-os": "^1.1.1"
  }
}
